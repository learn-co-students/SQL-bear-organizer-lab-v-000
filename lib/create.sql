CREATE TABLE bears(
  ID INTEGER PRIMARY KEY,
     name TEXT,
     age INTEGER,
     gender TEXT,
       -- m or f
     color TEXT,
     temperament TEXT,
     alive INTEGER
       -- 0(false) and 1 (true)
);
